version: 2.1

jobs:
  create_infrastructure:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      # - attach_workspace:
      #     at: ~/ci-meets-cd
      - run:
          name: create cloudformation stack
          command: |
            aws cloudformation create-stack --stack-name ci-meets-cd \ 
              --template-body file://aws/template.yml  \
              --parameters file://aws/template-parameters.json \
              --region=us-east-1
    
workflows:
  build:
    jobs:
      - create_infrastructure